# Screen Capture

When the user asks to look at the screen, see what's displayed, capture a screenshot, or check what an application is showing, use the **peekaboo** agent (via the Agent tool). The agent handles window listing, capture, and image analysis, returning a text description. Never capture screenshots directly in the main session -- delegate to the agent to avoid loading large image data into context.
