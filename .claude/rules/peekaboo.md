# Screen Capture

When the user asks to look at the screen, see what's displayed, capture a screenshot, or check what an application is showing, load the `peekaboo-cli` skill first. Never guess peekaboo commands — the skill has the correct usage.
